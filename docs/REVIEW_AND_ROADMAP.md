# مراجعة احترافية وخطة إعادة هيكلة وتحسين — تطبيق «ومضة» 🌟

> وثيقة تحليلية شاملة من منظور هندسة برمجيات + تصميم وتجربة مستخدم.
> أُعدّت بعد مراجعة فعلية للشيفرة وتشغيل أدوات التحليل والاختبار على البيئة المستهدفة.

---

## 1. الملخص التنفيذي

«ومضة» تطبيق Flutter تأملي غني بالمميزات (آية/دعاء اليوم، الورد، الإنجازات، التلاوة، المشاركة كصور، 5 لغات). البنية منظّمة بطبقات واضحة (`models / services / providers / screens / widgets / theme / l10n`) وهذا أساس جيد. لكن المراجعة كشفت عددًا من المشكلات التي تحدّ من «الاحترافية» المطلوبة:

| المحور | الحالة قبل | الحالة بعد هذا الـ PR |
|---|---|---|
| الاختبارات | اختبار واحد افتراضي **فاشل** (عدّاد غير موجود) | **28 اختبارًا ناجحًا** (ثيمات، Provider، نماذج) |
| تحليل الكود | 0 errors، 6 warnings، 380+ info (على 3.44) | **0 errors، 0 warnings** على 3.24.5 |
| ثيمات الألوان | 3 ثيمات (كلاسيكي/ليلي/وردي) | **6 ثيمات** + منتقي مرئي (إضافة: أخضر مريح، ذهبي، بيج) |
| توافق Flutter | ينكسر على إصدارات أحدث بسبب `intl` | نطاق مرن يدعم 3.24 والأحدث |

تبقى مشكلتان جوهريتان موصى بمعالجتهما لاحقًا: **تطبيق الثيم لا يَسري على كامل الواجهة** (ألوان ثابتة في 28 ملفًا)، و**ترجمات ناقصة** (67 مفتاحًا غير مترجم في fr/id/ur).

---

## 2. المنهجية والبيئة

- **بيئة الاختبار:** Flutter 3.24.5 / Dart 3.5.4 (الإصدار المستهدف في المشروع `>=3.24`).
- **الأدوات:** `flutter pub get` ← `flutter gen-l10n` ← `flutter analyze` ← `flutter test`.
- **ملاحظة توافق:** على Flutter 3.44 يظهر خطآن بنيويان (`CardTheme` → `CardThemeData`، وتوليد l10n) ناتجان عن تغيّرات API بين الإصدارات. القرار: اعتماد الإصدار المستهدف (3.24.5) لاختبار أمين للسلوك الأصلي، مع توسيع قيد `intl` ليعمل على الإصدارين معًا.

### القياس المرجعي (Baseline) قبل التعديل
- `flutter test`: **فشل** (الاختبار الوحيد كان قالب «العدّاد» الافتراضي ولا علاقة له بالتطبيق).
- `flutter analyze`: 6 تحذيرات (شيفرة ميتة + فحوص زائدة) و+380 ملاحظة (أغلبها `withOpacity` المهمل في الإصدارات الأحدث).

---

## 3. التقييم المعماري — نقاط القوة والضعف

### 3.1 نقاط القوة
- فصل طبقات واضح وأسماء معبّرة بالعربية، يسهّل الفهم.
- استخدام `Provider` لإدارة الحالة بشكل متّسق عبر `MultiProvider`.
- طبقة بيانات محلية (`assets/data/*.json`) تجعل التطبيق يعمل دون إنترنت.
- دعم RTL/LTR و5 لغات، وبنية l10n قياسية عبر ملفات `.arb`.

### 3.2 نقاط الضعف (مرتّبة حسب الأثر)

1. **مصدرا حقيقة للثيم (Single Source of Truth مفقود).**
   يوجد تعريف الألوان في مكانين: `ThemeColors` (المستخدم فعليًا في `AppTheme`) و`AppColors.getPrimaryColor/Secondary` (مفاتيح نصية). أي ثيم جديد يتطلب التحديث في عدة أماكن، ما يزيد احتمال التضارب.
   - *عولج جزئيًا:* الثيمات الجديدة أُضيفت في الموضعين، والاختبارات تحرس التطابق. *يبقى:* توحيد المصدر في `ThemeColors` وجعل `AppColors` يفوّض إليه.

2. **الثيم لا يَسري على كامل الواجهة (الأخطر بصريًا).**
   **28 ملفًا** في `screens/` و`widgets/` تستخدم `AppColors.primaryGreen` / `primaryGold` مباشرةً بدلًا من ألوان الثيم النشط أو `Theme.of(context).colorScheme`. النتيجة: تبديل الثيم (أخضر/ذهبي/بيج/ليلي/وردي) يغيّر شريط التطبيق والأزرار فقط، بينما تبقى البطاقات والأيقونات خضراء/ذهبية.
   - *التوصية:* استبدال الألوان الثابتة بـ `Theme.of(context).colorScheme.primary/secondary` أو `ThemeColors(themeProvider.colorTheme)`.

3. **الخدمات Static (يصعب اختبارها وحقنها).**
   `VerseRepository`, `NotificationService`, `LocalStorageService` تعتمد دوالًا ثابتة (`static`) وحالة عامة. هذا يمنع الـ mocking ويجعل اختبارات الوحدة للمنطق الأعلى صعبة.
   - *التوصية:* تحويلها إلى كائنات تُحقن عبر `Provider`/الباني (Constructor Injection) لتسهيل الاختبار.

4. **شيفرة ميتة وتحذيرات.** دوال غير مستخدمة (`_buildHeader`، `_buildQuickStats`، `_buildExploreCard`) ومتغيّرات/حقول غير مستخدمة وفحوص null زائدة. *عولج جزئيًا في هذا الـ PR.*

5. **ترجمات ناقصة.** `flutter gen-l10n` يُبلّغ عن **67 رسالة غير مترجمة** لكل من fr/id/ur — تظهر للمستخدم بالعربية (لغة القالب) بدل لغته.

6. **لا يوجد CI.** لا تكامل مستمر يمنع تكرار «الاختبار الفاشل» أو ظهور أخطاء تحليل.

7. **ديون توافق الإصدارات.** الاعتماد على واجهات تتغيّر بين إصدارات Flutter (مثل `CardTheme`) دون اختبار CI متعدد الإصدارات.

---

## 4. ما تم تنفيذه فعليًا في هذا الـ PR

1. **3 ثيمات ألوان جديدة** مريحة للعين تُضاف إلى الثلاثة الحالية:
   - **الأخضر المريح (Sage)** — أخضر هادئ منخفض التشبع.
   - **الذهبي (Golden)** — ذهبي دافئ مع لمسة بنية.
   - **البيج (Beige/Sepia)** — رملي دافئ محايد، الأنسب لراحة العين والقراءة الطويلة.
2. **منتقي ثيمات مرئي احترافي:** بدل قائمة الراديو النصية، أصبح كل ثيم يُعرض بدائرتي معاينة (أساسي/ثانوي) + مؤشر اختيار، مع «معاينة لونية مصغّرة» بجانب الإعداد نفسه.
3. **توطين كامل** لأسماء/أوصاف الثيمات الجديدة في اللغات الخمس.
4. **بدائل احتياطية (fallbacks)** لأسماء الثيمات تعتمد على امتداد الـ enum، فلا تظهر قيم فارغة لو غاب مفتاح ترجمة.
5. **مجموعة اختبارات حقيقية (28 اختبارًا):** لوحات الألوان لكل ثيم، تمايز الألوان، بناء `ThemeData` فاتح/داكن، سلوك `ThemeProvider` مع تخزين مُحاكى، و(de)serialization لـ `VerseModel`.
6. **تنظيف التحذيرات** + توسيع قيد `intl` لتوافق أوسع.

> **الحفاظ على التوافق الرجعي:** أُضيفت الثيمات في نهاية الـ enum للحفاظ على فهارس التفضيلات المحفوظة (classic=0…)، مع اختبار يحرس هذا الترتيب.

---

## 5. خطة إعادة الهيكلة (مراحل، مع المفاضلات)

### المرحلة 0 — تثبيت الجودة (أساس لكل ما بعده)
- إضافة **GitHub Actions**: `flutter analyze` + `flutter test` على كل PR (مصفوفة إصدارات 3.24 والأحدث).
- تفعيل قواعد lint أكثر صرامة تدريجيًا في `analysis_options.yaml`.
- *الفشل المحتمل:* كسر البناء على إصدار أحدث ← يُكتشف مبكرًا عبر مصفوفة الإصدارات.

### المرحلة 1 — توحيد نظام الثيم (أعلى أثر بصري)
- جعل `ThemeColors` المصدر الوحيد، وتفويض `AppColors` إليه.
- استبدال الألوان الثابتة في الـ 28 ملفًا بـ `colorScheme`/`ThemeColors`.
- **3 مسارات ممكنة:**
  1. *استبدال يدوي تدريجي شاشة بشاشة* — أأمن، يسمح بمراجعة بصرية لكل شاشة (موصى به).
  2. *Extension على `BuildContext`* مثل `context.brand.primary` لتقليل اللفظ المتكرر.
  3. *إعادة كتابة شاملة دفعة واحدة* — أسرع لكنه عالي المخاطرة بصريًا.
  - **التوصية:** المسار (1)+(2): إضافة Extension ثم الترحيل تدريجيًا، مع لقطات شاشة (golden tests) لكل ثيم.

### المرحلة 2 — قابلية الاختبار (حقن الاعتماديات)
- تحويل الخدمات الـ static إلى كائنات تُحقن، وكتابة اختبارات لـ `VerseRepository`/منطق «آية اليوم».

### المرحلة 3 — اكتمال الترجمة والوصولية
- ترجمة الـ 67 مفتاحًا الناقصة، وإضافة `untranslated-messages-file` لرصد النواقص.
- فحص تباين الألوان (WCAG AA) لكل ثيم في الوضعين.

---

## 6. تحسينات تجربة المستخدم والتصميم (UX/UI)

- **معاينة فورية للثيم:** عند فتح المنتقي، عرض بطاقة آية مصغّرة تتلوّن مباشرة بالثيم المختار قبل التأكيد.
- **اتباع ثيم النظام:** خيار «تلقائي» (`ThemeMode.system`) إلى جانب الفاتح/الداكن.
- **وضع القراءة المريح:** سطوع/تباين أهدأ + خلفية بيج (سيبيا) للجلسات الطويلة — الثيم الجديد «البيج» يخدم هذا مباشرة.
- **تكبير الخط الحي:** معاينة حجم الخط على نص آية فعلي داخل نافذة الاختيار.
- **اتساق المسافات والحواف:** توحيد `borderRadius`/الـ paddings عبر ثوابت تصميم (Design Tokens).
- **حالات فارغة وأخطاء أوضح:** رسائل موحّدة عبر `EmptyState` مع إجراء مقترح (CTA).
- **Haptics وانتقالات Hero** عند فتح تفاصيل الآية لإحساس أرقى.
- **إتاحة (Accessibility):** Semantics labels للأيقونات، واحترام `MediaQuery.textScaler` بدل `textScaleFactor` المهمل.

---

## 7. مرجع ثيمات الألوان الجديدة

| الثيم | أساسي | ثانوي | الاستخدام الأمثل |
|---|---|---|---|
| الأخضر المريح (Sage) | `#4A7C59` | `#C2A878` | يومي هادئ، منخفض التشبع |
| الذهبي (Golden) | `#B8860B` | `#6D4C1E` | إحساس فاخر/مناسبات |
| البيج (Beige) | `#8C7B5B` | `#B89B72` | **راحة العين والقراءة الطويلة** |
| الكلاسيكي | `#2D5F3F` | `#D4AF37` | الهوية الأصلية (أخضر/ذهبي) |
| الليلي | `#1A237E` | `#90A4AE` | أزرق/فضي |
| الوردي | `#880E4F` | `#BA68C8` | وردي/بنفسجي |

> **إرشاد تباين:** يُنصح قبل الإطلاق بفحص نسبة التباين (≥ 4.5:1 للنص العادي) للون الأساسي فوق الخلفيات الفاتحة/الداكنة، خاصةً «الذهبي» الذي قد يحتاج تغميقًا فوق الأبيض.

---

## 8. مقترحات «قيمة مضافة» للتطبيق

- **تتبّع الختمة/الورد الأسبوعي** مع رسوم تقدّم بسيطة.
- **تذكيرات ذكية حسب المناسبة** (رمضان، الجمعة، أيام بيض) مرتبطة بنظام المناسبات الموجود أصلًا.
- **مشاركة كصورة بثيم المستخدم النشط** (حاليًا القوالب ثابتة الألوان) لتناسق الهوية.
- **وضع «آية قبل النوم»** بألوان دافئة وسطوع منخفض تلقائيًا.
- **نسخة Web/PWA** (مذكورة كـ«قريبًا») للوصول الأوسع.
- **مزامنة المفضلة/التقدم اختياريًا** عبر تخزين سحابي خفيف.
- **ويدجت الشاشة الرئيسية** (مؤجّل في README) لعرض آية اليوم.

---

## 9. خارطة طريق مرتّبة بالأولوية

**P0 (تم في هذا الـ PR):** إصلاح الاختبار + مجموعة اختبارات، الثيمات الجديدة + منتقٍ مرئي، تنظيف التحذيرات، توافق `intl`.

**P1 (موصى به تاليًا):**
- CI (analyze + test).
- ترحيل الألوان الثابتة إلى الثيم (المرحلة 1).
- إكمال الترجمات الـ67.

**P2 (تحسينات):**
- حقن الخدمات (قابلية الاختبار).
- معاينة الثيم الحيّة + `ThemeMode.system`.
- مشاركة الصور بثيم المستخدم.
- معالجة ملاحظات `withOpacity` المهملة دفعة واحدة عبر codemod.

---

## 10. نقاط الفشل المحتملة وكيف نكتشفها مبكرًا

| الخطر | الاكتشاف المبكر |
|---|---|
| كسر التوافق بين إصدارات Flutter | CI بمصفوفة إصدارات |
| تضارب ألوان الثيم بين المصدرين | اختبار `theme_colors_test.dart` (موجود) + توحيد المصدر |
| تغيّر فهارس الـ enum يفسد تفضيلات المستخدمين | اختبار ترتيب الـ enum (موجود) |
| تباين منخفض في ثيم معيّن | فحص WCAG قبل الإطلاق |
| ترجمات ناقصة تظهر بالعربية | `untranslated-messages-file` في `l10n.yaml` |

---

## 11. ملحق — كيفية التحقق محليًا

```bash
flutter pub get
flutter gen-l10n
flutter analyze   # متوقع: 0 errors، 0 warnings
flutter test      # متوقع: All tests passed (28)
```

> **آية تضيء يومك** — وثيقة حيّة، تُحدَّث مع تقدّم خارطة الطريق.

---

## 12. سجل التنفيذ — الجولة الثانية (تحديث)

نُفِّذت في هذه الجولة بنود رئيسية من خارطة الطريق أعلاه:

### ✅ P1 — تطبيق الثيم على كامل الواجهة (المرحلة 1)
- أُدخل مصدر موحّد للون الثيم النشط: `AppColors.activeTheme` مع `brandPrimary`/`brandSecondary`،
  ويُحدَّث تلقائيًا داخل `AppTheme.getLightTheme/getDarkTheme` عند كل إعادة بناء.
- رُحِّل **19 ملفًا** في `screens/` و`widgets/` من الألوان الثابتة (`primaryGreen`/`primaryGold`)
  إلى ألوان الثيم النشط؛ فأصبح تبديل الثيم (أخضر/ذهبي/بيج/ليلي/وردي) يَسري على البطاقات
  والأيقونات والأزرار في كامل التطبيق.
- قوالب المشاركة (`share_templates`/`dua_share_templates`) أُبقيت بهوياتها اللونية المستقلة عمدًا.

### ✅ P1 — إكمال الترجمات
- تُرجمت **67 رسالة** ناقصة لكل من الفرنسية والإندونيسية والأردية (المجموع 201 سلسلة)،
  وأصبح `flutter gen-l10n` يعمل دون أي رسائل غير مترجمة.

### ✅ P0/P1 — التكامل المستمر (CI)
- أُضيف `.github/workflows/ci.yml` يشغّل `flutter analyze` + `flutter test`
  على مصفوفة إصدارات (`3.24.5` حاجب + `stable` كإنذار مبكر غير حاجب).

### ✅ تحسينات UX/UI
- دعم **وضع السمة التلقائي** (`ThemeMode.system`) إلى جانب الفاتح/الداكن مع حفظ التفضيل وتوافق رجعي.
- **معاينة حية** لثيم الألوان داخل المنتقي (بطاقة تتلوّن فورًا مع الاختيار).
- استبدال `textScaleFactor` المهمل بـ `TextScaler.linear` (إتاحة/توافق مستقبلي).

### ✅ إصلاح خلل المعاينة
- معاينة مشاركة الآية/الدعاء كانت تظهر ممدودة/مشوّهة بسبب `Transform.scale`
  (يُبقي حجم التخطيط 1080×1920). استُبدل بـ `FittedBox(BoxFit.contain)` بحدود مضبوطة،
  فصارت المعاينة مطابقة تمامًا للصورة التي تُشارَك.

### الاختبارات والجودة بعد الجولة الثانية
- `flutter analyze`: **0 errors، 0 warnings** (ملاحظات info فقط).
- `flutter test`: **31 اختبارًا ناجحًا** (أُضيفت اختبارات الثيم النشط ووضع السمة).

### ما يزال مفتوحًا (P2)
- حقن الخدمات الـ static (قابلية الاختبار) — المرحلة 2.
- مشاركة الصور بثيم المستخدم النشط، ووضع «ما قبل النوم».
- معالجة ملاحظات `withOpacity` المهملة دفعةً واحدة (codemod) عند الترقية لإصدار Flutter أحدث.
- نسخة Web/PWA، ومزامنة سحابية اختيارية، وويدجت الشاشة الرئيسية.
